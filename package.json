{
  "name": "git-daily",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Git Daily — Learn Git like reading the morning paper.",
  "scripts": {
    "dev": "vite",
    "test": "node --test",
    "build": "vite build",
    "preview": "vite preview",
    "sitemap": "node scripts/build-sitemap.mjs"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.7"
  }
}
